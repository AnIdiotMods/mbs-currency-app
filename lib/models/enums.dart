enum ProgramType { mbs, mgs }

enum PartyAffiliation { nationalist, federalist }

enum AccountType { personal, corporate, admin }

enum TransactionType { transfer, payment, adminAdjustment }

enum TransactionStatus { completed, failed }
